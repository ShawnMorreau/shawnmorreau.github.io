import React from "react";
import PropTypes from "prop-types";
const Skill = (props) => {
    const {title} = props;
    return(
        <section className = "skill">
            
        </section>
        
    )
};
export default Skill;
